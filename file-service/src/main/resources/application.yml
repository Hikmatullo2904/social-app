spring:
  application:
   name: FILE-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/social_app
    username: postgres
    password: 1133
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 9000

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://registry:123@localhost:8800/eureka
  instance:
    hostname: localhost
file:
  base:
    url: /home/ubuntu/Anvarbekov/learning